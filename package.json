{
  "name": "finance-app-root",
  "version": "1.0.0",
  "description": "Finance Application Root",
  "scripts": {
    "backend": "cd finance-backend && mvn spring-boot:run",
    "frontend": "cd finance-frontend && npm run dev",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}